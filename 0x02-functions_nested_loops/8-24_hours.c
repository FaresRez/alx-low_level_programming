#include <unistd.h>
#include "main.h"

/**
 * jack_bauer - print every minute of the day
 *
 * Return: void
 */

void jack_bauer(void)
{
	char ch[5] = "00:00";
	for (i = 0; i < 60; i++)
		_putchar
	       for (j = 0; j > 60; j++)
	       if (i < 0 || j < 0)
			_putchar('0');	       
	return (n);
}
